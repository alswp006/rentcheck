# Testing Rules
- Use vitest with jsdom environment
- Use @testing-library/react for component tests
- Test files in src/__tests__/
- Run: vitest run (single) or vitest (watch mode)
- Coverage not required for mini apps
