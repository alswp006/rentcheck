workspaces=false
# @apps-in-toss/ait-format declares engines: {node: '>=24'} which is stricter
# than most CI environments (Node 18/20/22). Disable strict engine checks so
# installs succeed on LTS Node versions while the upstream fixes their engines field.
engine-strict=false
